import { UiModeDirective } from './ui-mode.directive';

describe('UiModeDirective', () => {
  it('should create an instance', () => {
    const directive = new UiModeDirective();
    expect(directive).toBeTruthy();
  });
});
